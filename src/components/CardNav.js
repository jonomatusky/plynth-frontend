import React from 'react'
import { Paper, Button, Typography } from '@material-ui/core'
import Emoji from './Emoji'

const CardNavButton = (card) => {
  if 
}

const CardNav = () => {
  return (
    <Paper>
      <Button>
        <Typography>
          <Emoji label=""/>
        </Typography>
      </Button>
    </Paper>
  )
}

export default CardNav
